/**
 * Drive-Thru Layout - Wraps the main ordering interface
 * This is the interface customers use to place orders via voice AI
 * DataProvider is now in the root layout
 */
export default function DriveThruLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

