import MainLayout from '@/components/MainLayout';

/**
 * Root page - Main drive-thru interface
 * This is the customer-facing ordering interface with voice AI capabilities
 * The DataProvider is available through the (drive-thru) layout
 */
export default function Home() {
  return <MainLayout />;
}
